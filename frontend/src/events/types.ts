import { PublicLanguage } from '../localization/languages';export type EventLocale=PublicLanguage;export interface PublicEvent{id:string;slug:string;title:string;description:string;organizer:string;location:string;imageUrl?:string;imageAlt?:string;startAt:string;endAt:string;eventStatus:'scheduled'|'postponed'|'cancelled';temporalStatus:'upcoming'|'past';category:{slug:string;label:string};featured:boolean;kideAppUrl?:string;locale:EventLocale}export interface EventCategory{id:string;slug:string;labels:{en:string;sv:string}}

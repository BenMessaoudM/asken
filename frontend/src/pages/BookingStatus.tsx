import { FormEvent,useState } from 'react';import { getBookingStatus } from '../booking/api';import { bookingContent } from '../booking/content';import { BookingLocale,BookingStatusRecord } from '../booking/types';import PageHero from '../components/PageHero';import PublicLayout from '../components/PublicLayout';import { usePageMetadata } from '../hooks/usePageMetadata';import { useSiteLocale } from '../hooks/useSiteLocale';import { bookingLanguages, languageLabels } from '../localization/languages';import { formatDateTime } from '../utils/dateTime';
export default function BookingStatus(){const{locale}=useSiteLocale(),[bookingLocale,setBookingLocale]=useState<BookingLocale>(locale),copy=bookingContent[bookingLocale],[reference,setReference]=useState(''),[email,setEmail]=useState(''),[booking,setBooking]=useState<BookingStatusRecord|null>(null),[error,setError]=useState(''),[loading,setLoading]=useState(false);usePageMetadata(copy.statusLink,copy.intro,'/booking/status');const submit=async(event:FormEvent)=>{event.preventDefault();setLoading(true);setError('');try{setBooking((await getBookingStatus(reference.trim().toUpperCase(),email.trim(),bookingLocale)).data.booking)}catch{setBooking(null);setError(copy.error)}finally{setLoading(false)}};return <PublicLayout><PageHero compact eyebrow={copy.eyebrow} title={copy.statusLink} description={copy.intro}/><div className="ask-container py-14 sm:py-20"><form onSubmit={submit} className="ask-card mx-auto grid max-w-2xl gap-5 p-6 sm:p-9"><label className="font-semibold">{copy.language}<select value={bookingLocale} onChange={event=>setBookingLocale(event.target.value as BookingLocale)} className="mt-2 w-full rounded-xl border p-3">{bookingLanguages.map(language=><option key={language} value={language}>{languageLabels[language]}</option>)}</select></label><label className="font-semibold">{copy.reference}<input required pattern="COR-[0-9]{4}-[0-9]{4}" placeholder="COR-2026-0001" value={reference} onChange={event=>setReference(event.target.value)} className="mt-2 w-full rounded-xl border p-3 uppercase"/></label><label className="font-semibold">{copy.email}<input required type="email" value={email} onChange={event=>setEmail(event.target.value)} className="mt-2 w-full rounded-xl border p-3"/></label>{error&&<p role="alert" className="rounded-xl bg-red-50 p-4 text-red-800">{error}</p>}<button disabled={loading} className="ask-button-primary">{loading?'…':copy.check}</button></form>{booking&&<article className="ask-card mx-auto mt-8 max-w-2xl p-6 sm:p-9" aria-live="polite"><div className="flex flex-wrap justify-between gap-3"><h2 className="text-2xl font-black">{booking.resource.name}</h2><strong className="rounded-full bg-ask-50 px-4 py-2 text-ask-600">{copy.statuses[booking.status]}</strong></div><dl className="mt-6 grid gap-5 sm:grid-cols-2"><div><dt className="font-bold">{copy.reference}</dt><dd>{booking.reference}</dd></div><div><dt className="font-bold">{copy.bookingType}</dt><dd>{copy.types[booking.bookingType]}</dd></div><div><dt className="font-bold">{copy.start}</dt><dd>{formatDateTime(booking.startAt)}</dd></div><div><dt className="font-bold">{copy.end}</dt><dd>{formatDateTime(booking.endAt)}</dd></div><div><dt className="font-bold">{copy.total}</dt><dd>{booking.price.totalPrice.toFixed(2)} €</dd></div><div className="sm:col-span-2"><dt className="font-bold">{copy.purpose}</dt><dd>{booking.purpose}</dd></div>{booking.publicNotes&&<div className="rounded-xl bg-ask-50 p-4 sm:col-span-2"><dt className="font-bold">ASK</dt><dd>{booking.publicNotes}</dd></div>}</dl></article>}</div></PublicLayout>}

export type EventLocale='en'|'sv';export interface EventTranslation{title:string;description:string;organizer:string;location:string;imageAlt?:string}export interface EventCategory{id:string;slug:string;labels:{en:string;sv:string}}export interface ManagedEvent{id:string;slug:string;publicationStatus:'draft'|'published';temporalStatus:'upcoming'|'past';version:number;translations:Record<EventLocale,EventTranslation>;imageUrl?:string;startAt:string;endAt:string;category:EventCategory;eventStatus:'scheduled'|'postponed'|'cancelled';featured:boolean;kideAppUrl?:string}

import{describe,expect,it}from'vitest';import{defaultOpeningHours,emptyResource,localDate,statusTone}from'./forms';describe('booking forms',()=>{it('creates independent bilingual fields',()=>{const a=emptyResource(),b=emptyResource();a.name.en='Room';expect(b.name.en).toBe('')});it('creates weekday opening hours',()=>expect(defaultOpeningHours()).toHaveLength(5));it('formats local date input',()=>expect(localDate('2030-01-01T10:00:00Z')).toMatch(/^2030-01-01T/));it('maps statuses to visible tones',()=>expect(statusTone('approved')).toContain('green'))});
